type Animal {
    id: Int!,
    name: String!, 
    age: Int!
}

type Query{
    animals: [Animal]!,
    getAnimals(id: Int!): Animal ,
    getAllAnimals: [Animal]
}

type Mutation {
    addAnimal(name: String!, age: Int!):Animal!
}